# ADR-004: Optional AI provider port

Status: accepted. Use an application port with replaceable Ollama and local OpenAI-compatible adapters when a concrete AI use case is built. The service must run without either provider. Store provenance, verification and approval metadata for generated suggestions. Spring AI is compatible but deferred from Phase 1 because there is no call site.
