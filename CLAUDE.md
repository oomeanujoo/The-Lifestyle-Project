# Claude project entry

Read `AGENTS.md`, then `docs/agent-context/START-HERE.md`.
Follow the same approval, secret-handling, and Git restrictions.
At the start of each session, read `agent-tasks/claude-task.md`. If its status
is READY, take that as your assigned task. Work only within its file ownership.
Record results in `agent-tasks/claude-result.md` and mark the task DONE when
finished. A synced task file is not permission to contact a Git remote or
change system settings on the office laptop.
Before starting each next READY task, check for
`agent-tasks/claude-takeover-task.md`. If it exists and is READY, follow the
takeover rule in `agent-tasks/README.md` and handle it first. Review Cline's
result when directed, but do not edit Cline-owned files until its affected
task is CANCELLED and the active Cline session has acknowledged stopping.
Then read `agent-tasks/claude-next-task.md`. If its status is READY, start it
in the same session, following its separate ownership and result instructions.
After that task is DONE, read `agent-tasks/claude-03-task.md`, then
`agent-tasks/claude-04-task.md`. Start each READY task in order, recording its
own result before proceeding. Neither task depends on Cline finishing.
Keep the current architecture
in `TECHNICAL_ARCHITECTURE.md` accurate when architecture changes, and record
the reasoning for a significant change in `TECHNICAL_ARCHITECTURE_DECISION_LOG.md`.
Do not edit, commit, or push without the required separate permissions.
